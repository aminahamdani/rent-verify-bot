# SMS Blueprint Architecture Diagram

## High-Level Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                      Twilio Service                              │
│                  (External Service)                              │
└────────────────────┬────────────────────────────────────────────┘
                     │
                     │ POST /sms webhook
                     │ From=+1234567890, Body=YES
                     ▼
┌─────────────────────────────────────────────────────────────────┐
│                    Flask Application                             │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │          routes/sms.py (SMS Blueprint)                    │  │
│  │                                                            │  │
│  │  @sms_bp.route('/sms', methods=['POST'])                  │  │
│  │  def sms_reply():                                         │  │
│  │    - Extract SMS data                                     │  │
│  │    - Lazy import app helpers                             │  │
│  │    - Mask phone number (if production)                   │  │
│  │    - Insert into database                                │  │
│  │    - Return response                                      │  │
│  └───────────────────────────────────────────────────────────┘  │
└────────┬──────────────────────────────────────────────────┬─────┘
         │                                                  │
         ▼                                                  ▼
┌────────────────────────┐                    ┌────────────────────────┐
│    app.py              │                    │    app_local.py        │
│  (PostgreSQL)          │                    │    (SQLite)            │
│                        │                    │                        │
│ register_blueprint()   │                    │ register_blueprint()   │
│ get_db_connection()    │                    │ get_db_connection()    │
│ mask_phone_number()    │                    │ (returns raw number)   │
│ logger                 │                    │ logger                 │
└────────┬───────────────┘                    └────────┬───────────────┘
         │                                            │
         ▼                                            ▼
┌────────────────────────┐                    ┌────────────────────────┐
│  PostgreSQL Database   │                    │   SQLite Database      │
│                        │                    │                        │
│ rent_records:          │                    │ rent_records:          │
│ - phone_number (masked)│                    │ - phone_number (raw)   │
│ - reply                │                    │ - reply                │
│ - timestamp            │                    │ - timestamp            │
└────────────────────────┘                    └────────────────────────┘
```

---

## Request Lifecycle

```
1. TWILIO WEBHOOK
   ┌──────────────────────────────┐
   │ SMS from User Phone Number   │
   │ Message Body: "YES" or "NO"  │
   └──────────────┬───────────────┘

2. HTTP REQUEST
   │  POST /sms
   │  From: +1234567890
   │  Body: YES
   └──────────────┬───────────────

3. ROUTE HANDLER (sms_reply in routes/sms.py)
   ┌──────────────────────────────┐
   │ Extract SMS Parameters       │
   │ - raw_phone_number           │
   │ - reply (message body)       │
   │ - timestamp (created)        │
   └──────────────┬───────────────┘

4. LAZY IMPORT LOGIC
   │
   ├─→ Try: from app import helpers (Production)
   │   └─→ use_mask = True
   │       phone masking enabled
   │
   └─→ Except: from app_local import helpers (Local)
       └─→ use_mask = False
           phone masking disabled

5. DATABASE OPERATION
   ┌──────────────────────────────┐
   │ Get DB Connection            │
   │ Create Cursor                │
   │ Execute INSERT Statement     │
   │ - PostgreSQL: %s parameters  │
   │ - SQLite: ? parameters       │
   │ Commit Transaction           │
   └──────────────┬───────────────┘

6. RESPONSE
   │  ✓ Success: "Reply recorded" (200)
   │  ✗ Error: "Error processing message" (500)
   └──────────────┬───────────────

7. DATABASE STATE
   ┌──────────────────────────────┐
   │ rent_records table updated:  │
   │ phone_number: ******9380     │
   │ reply: YES                   │
   │ timestamp: 2024-01-16 14:30  │
   └──────────────────────────────┘
```

---

## File Structure

```
RentVerify/
│
├── 📄 app.py                     (Production Flask app - PostgreSQL)
│   └── Imports & Registers sms_bp
│
├── 📄 app_local.py               (Local Flask app - SQLite)
│   └── Imports & Registers sms_bp
│
├── 📁 routes/                    (New Routes Package)
│   ├── 📄 __init__.py            (NEW - Package initialization)
│   │   └── Exports: sms_bp
│   │
│   └── 📄 sms.py                 (SMS Blueprint)
│       └── Blueprint: sms_bp
│           └── Route: POST /sms
│
├── 📁 instance/                  (Database)
│   └── rent_data.db             (SQLite database - local only)
│
├── 📄 BLUEPRINT_REFACTORING.md   (NEW - Full documentation)
├── 📄 SMS_BLUEPRINT_GUIDE.md     (NEW - Quick reference)
└── 📄 REFACTORING_VALIDATION.md  (NEW - Validation report)
```

---

## Component Interactions

### Lazy Import Pattern

```python
┌─────────────────────────────────────┐
│   Request to POST /sms endpoint     │
└────────────┬────────────────────────┘
             │
             ▼
┌─────────────────────────────────────┐
│  sms_reply() function executes      │
└────────────┬────────────────────────┘
             │
             ▼
    ┌────────────────────┐
    │  Try to import     │
    │  from app (Prod)   │
    └─┬──────────────┬───┘
      │              │
      │ Success      │ Failure
      ▼              ▼
   ┌────────┐    ┌─────────────┐
   │ Prod   │    │ Local       │
   │ Mode   │    │ Fallback    │
   └────────┘    └─────────────┘
      │              │
      └──────┬───────┘
             ▼
    ┌────────────────────┐
    │ Get DB Connection  │
    │ (Adapts to env)    │
    └────────────────────┘
```

---

## Database Abstraction

```
┌──────────────────────────────────────────────────────┐
│        SMS Blueprint (routes/sms.py)                 │
│  Abstract Interface - Works with both databases      │
└───────────────────┬──────────────────────────────────┘
                    │
        ┌───────────┴───────────┐
        │                       │
        ▼                       ▼
┌──────────────────────┐  ┌──────────────────────┐
│  PostgreSQL Path     │  │  SQLite Path         │
│                      │  │                      │
│ • Connection pooling │  │ • Direct connection  │
│ • %s parameters      │  │ • ? parameters       │
│ • Masked numbers     │  │ • Raw numbers        │
│ • Pool cleanup       │  │ • Direct close()     │
└──────────────────────┘  └──────────────────────┘
        │                       │
        ▼                       ▼
   ┌─────────────┐          ┌──────────┐
   │ PostgreSQL  │          │ SQLite   │
   │ Database    │          │ Database │
   └─────────────┘          └──────────┘
```

---

## Phone Number Masking Flow

```
Raw Phone: +1234567890

┌────────────────────────────────────────────┐
│  Is Production Environment?                │
└──────┬───────────────────────────┬─────────┘
       │ Yes (app.py)             │ No (app_local.py)
       ▼                           ▼
    MASK IT                    KEEP RAW
    ┌──────────┐              ┌──────────────┐
    │ ······9890              │ +1234567890  │
    │ (Masked)                │ (Raw)        │
    └──────┬───┘              └──────┬───────┘
           │                         │
           └────────────┬────────────┘
                        ▼
         Store in Database & Display
```

---

## Error Handling Flow

```
┌─────────────────────────────────┐
│  SMS Request to /sms            │
└────────────┬────────────────────┘
             │
             ▼
    ┌────────────────────────┐
    │  Try to process SMS    │
    └──────┬─────────────┬───┘
           │             │
           │ Error       │ Success
           ▼             ▼
       ┌────────┐    ┌─────────┐
       │ Log    │    │ Save to │
       │ Error  │    │ DB      │
       │        │    │         │
       │ Return │    │ Return  │
       │ 500    │    │ 200     │
       └────────┘    └─────────┘
           │             │
           └─────┬───────┘
                 ▼
        ┌──────────────────┐
        │ Finally Block:   │
        │ Cleanup DB Conn  │
        │ (Always runs)    │
        └──────────────────┘
```

---

## Module Dependencies

```
┌─────────────────────────────────────────┐
│   routes/sms.py (SMS Blueprint)         │
│                                         │
│   Dependencies:                         │
│   ├── Flask (Blueprint, request)        │
│   ├── datetime (timestamp)              │
│   ├── app.py (lazy import - optional)   │
│   └── app_local.py (lazy import - opt)  │
└─────────────────────────────────────────┘
         ▲               ▲
         │               │
    ┌────┘               └────┐
    │                         │
    ▼                         ▼
┌──────────────┐      ┌──────────────────┐
│   app.py     │      │   app_local.py   │
│              │      │                  │
│ Imports:     │      │ Imports:         │
│ • sms_bp     │      │ • sms_bp         │
│              │      │                  │
│ Registers:   │      │ Registers:       │
│ • Blueprint  │      │ • Blueprint      │
└──────────────┘      └──────────────────┘
```

---

## State Transitions

```
Before Refactoring (Monolithic):
┌─────────────────────┐
│   app.py / app_local.py
│  (SMS logic inside) │
└─────────────────────┘

After Refactoring (Modular):
┌──────────────────┐       ┌──────────────────┐
│   app.py         │       │   app_local.py   │
│  (Core logic)    │       │  (Core logic)    │
└────────┬─────────┘       └────────┬─────────┘
         │                          │
         │  register_blueprint()    │
         │  register_blueprint()    │
         │                          │
         └──────────────┬───────────┘
                        ▼
              ┌──────────────────────┐
              │  routes/sms.py       │
              │  (SMS Blueprint)     │
              │  (Reusable, Modular) │
              └──────────────────────┘
```

---

## Execution Timeline (Happy Path)

```
Time  Event                              Component
───────────────────────────────────────────────────────────
 t0   Twilio sends SMS                   Twilio Service
 t1   HTTP POST /sms request             Network
 t2   Flask routes to sms_reply()        Flask Router
 t3   Extract parameters                 SMS Blueprint
 t4   Lazy import app helpers            SMS Blueprint
 t5   Get database connection            Database Layer
 t6   Mask phone number (if needed)      SMS Blueprint
 t7   Create INSERT statement            SMS Blueprint
 t8   Execute INSERT query               Database Driver
 t9   Commit transaction                 Database Driver
t10   Return success response            Flask Response
t11   Database cleanup                   SMS Blueprint
```

---

## Summary

This modular architecture provides:
- ✅ Clean separation of concerns
- ✅ Reusable blueprint structure
- ✅ Dual-environment support
- ✅ Graceful error handling
- ✅ Maintainable codebase
- ✅ Scalable for future features
